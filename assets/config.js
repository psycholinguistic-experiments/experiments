/* Class data connection.
   Paste the Google Apps Script web-app URL between the quotes (it ends in /exec).
   Leave it empty and the tasks still run: results stay on each student's device
   and can be downloaded as CSV. Setup steps: README.md */
window.LT5461_CONFIG = {
    endpoint: ''
};
